#!/bin/sh
set -eu
set -o pipefail

case $1 in
    up)
        pactl set-sink-mute 0 0
        pactl set-sink-volume 0 +5%
        ;;
    down)
        pactl set-sink-mute 0 0
        pactl set-sink-volume 0 -5%
        ;;
    mute)
        pactl set-sink-mute 0 toggle
        ;;
esac
